package Example.interfaces;

/**
 * Movable adapter interface;
 * Based on the same Movable interface, can be modified to yield different results
 */
public interface MovableAdapter {

    /**
     * getSpeed from MovableAdapter
     * @return Speed in kilometers per hour
     */
    double getSpeed();
}
