package Example.interfaces;

/**
 * original interface Movable, that's returning the speed of luxury cars in miles per hour
 */
public interface Movable {

    /**
     * getSpeed from Movable
     * @return Speed in miles per hour
     */
    double getSpeed();
}
